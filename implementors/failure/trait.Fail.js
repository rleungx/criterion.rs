(function() {var implementors = {};
implementors["criterion_plot"] = [{text:"impl Fail for <a class=\"enum\" href=\"criterion_plot/enum.VersionError.html\" title=\"enum criterion_plot::VersionError\">VersionError</a>",synthetic:false,types:["criterion_plot::VersionError"]},];

            if (window.register_implementors) {
                window.register_implementors(implementors);
            } else {
                window.pending_implementors = implementors;
            }
        
})()
